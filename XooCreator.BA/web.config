<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <system.webServer>
    <security>
      <requestFiltering>
        <!-- 600MB în bytes (600 * 1024 * 1024 = 629145600) -->
        <!-- This is required for Azure App Service (IIS) to allow large file uploads -->
        <requestLimits maxAllowedContentLength="629145600" />
      </requestFiltering>
    </security>
  </system.webServer>
</configuration>

